<%= form_for(@rol) do |f| %>
  <% if @rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rol.errors.count, "error") %> prohibited this rol from being saved:</h2>

      <ul>
      <% @rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre ,"required" => "required"%>
  </div>
  <div class="field">
    <%= f.label :precio_hora %><br>
    <%= f.number_field :precio_hora ,"required" => "required", "step"=>"any"%>
  </div>
  <div class="actions">
    <%= f.submit "Guardar"%>
  </div>
<% end %>
